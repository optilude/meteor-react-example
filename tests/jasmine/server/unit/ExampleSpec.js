/* globals describe, beforeEach, it, expect, spyOn, MeteorStubs */
"use strict";

describe('Example', function() {

    it('should be able to add two numbers together', function() {
        var result = 2 + 2;
        expect(result).toEqual(4);
    });

});
